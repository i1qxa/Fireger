# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar

# IntelliJ IDEA
.idea/
*.iml
*.iws
*.ipr

# Service account keys
config/service-accounts/*.json
projects.json

# Local DB (Exposed/H2)
data/

# Logs
*.log

# OS
.DS_Store
Thumbs.db

# Local overrides (токен, URL сервера — не коммитить при локальной подстановке)
.env
.env.local
application.local.conf
config.local.*
**/secrets*

# SSL keystore (генерируется при первом запуске)
keystore.jks
